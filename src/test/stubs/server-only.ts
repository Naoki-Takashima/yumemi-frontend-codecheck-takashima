/**
 * `server-only` のテスト用スタブ。
 *
 * 本物の `server-only` は「react-server」条件を持たない環境で読み込まれると
 * 例外を投げる。これはクライアントバンドルへの混入を防ぐための仕組みで、
 * 本番では期待どおりの挙動だが、Vitest から Route Handler を検証できなくなる。
 *
 * vitest.config.mts でこのファイルにエイリアスし、テスト時だけ無害化する。
 * 本番のビルドはエイリアスを通らないため、混入検知は効いたまま。
 */
export {};
